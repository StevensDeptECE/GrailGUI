#include "opengl/MenuWidget.hh"

#include "opengl/MultiText.hh"
#include "opengl/StyledMultiShape2D.hh"

using namespace std;

void ButtonWidget::init() {
  // TODO: draw the ButtonWidget
  // m->drawRectangle(x, y, w,);
  // m->fillRectangle(x, );
  // t->text(...   text ...);
}